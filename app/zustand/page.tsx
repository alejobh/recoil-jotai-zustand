import { LibHeader } from '../_components/LibHeader';
import { PageContainer } from '../_components/PageContainer';
import { ToastContainer } from './ToastContainer';

const Zustand = () => {
  return (
    <PageContainer>
      <LibHeader name="Zustand" />
      <ToastContainer />
    </PageContainer>
  );
};

export default Zustand;
